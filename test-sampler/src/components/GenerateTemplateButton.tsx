import React from 'react'
import '../styles/button.css'

const GenerateTemplateButton = ({ onClick }: any) => {

  return (
    <button onClick={onClick}>Generate Template</button>
  )
}

export default GenerateTemplateButton